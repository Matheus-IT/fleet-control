export interface VehicleEntry {
  state: string;
  licencePlate: string;
  model: string;
  description: string;
  author: string;
  date: string;
}

export interface VehicleEntryDTO {
  state: string;
  licence_plate: string;
  model: string;
  description: string;
  author: string;
  date: string;
}
